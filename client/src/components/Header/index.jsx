import React from 'react';

// elements
import { HeaderContainer, HeaderTitle } from './HeaderElements';

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Reminder</HeaderTitle>
    </HeaderContainer>
  );
};

export default Header;
